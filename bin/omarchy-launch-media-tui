#!/usr/bin/env bash
set -euo pipefail

exec setsid uwsm-app -- alacritty \
  --class org.omarchy.media-popup \
  --title "Media" \
  -o window.dimensions.columns=64 \
  -o window.dimensions.lines=15 \
  -e omarchy-media-tui
